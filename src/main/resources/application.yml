spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: secret
    username: myuser
    url: jdbc:postgresql://localhost/mydatabase
  application:
    name: orders-service

generate:
  endpoint: http://localhost:8080/numbers/generate